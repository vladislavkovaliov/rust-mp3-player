export component OpenFolderButton inherits Rectangle {
    width: 16px;
    height: 16px;
    border-color: grey;
    border-width: 1px;
    border-radius: self.width / 2;
    opacity: 0.7;

    Image {
        width: 10px;
        height: 10px;
        source: @image-url("../icons/folder.svg");
    }
}
